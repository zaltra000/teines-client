import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tiens.healthwealth',
  appName: 'tiens-health-wealth',
  webDir: 'dist'
};

export default config;
